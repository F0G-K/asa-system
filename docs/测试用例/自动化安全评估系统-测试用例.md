本文档约 2500 字，阅读本文档约 5 分钟。

# 自动化安全评估系统测试用例

## 1. 认证与初始化

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| AUTH-01 | 系统未初始化时创建管理员 | P0 | `201`，密码仅存 Argon2id 哈希，日志无明文 |
| AUTH-02 | 已初始化后重复初始化 | P0 | `409 SYSTEM_ALREADY_INITIALIZED` |
| AUTH-03 | 并发初始化 | P0 | 仅创建一个管理员 |
| AUTH-04 | 正确凭证登录 | P0 | `200`，`Set-Cookie` 含 `asa_session`(HttpOnly)、`asa_csrf` |
| AUTH-05 | 错误凭证登录 | P0 | `401 INVALID_CREDENTIALS`，不区分用户不存在与密码错误 |
| AUTH-06 | 禁用账户登录 | P1 | `403 ACCOUNT_DISABLED` |
| AUTH-07 | 未登录访问受保护接口 | P0 | `401 AUTH_REQUIRED` |
| AUTH-08 | 会话过期后访问 | P0 | `401 SESSION_EXPIRED` |
| AUTH-09 | 退出登录 | P0 | `200`，Cookie 清除，重复退出幂等 |
| AUTH-10 | 缺 `X-CSRF-Token` 写操作 | P0 | `403 CSRF_INVALID` |
| AUTH-11 | 登录限流 | P1 | `429 RATE_LIMITED` + `Retry-After` |
| AUTH-12 | 未初始化时登录 | P0 | `409 SYSTEM_NOT_INITIALIZED` |

## 2. 项目管理

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| PROJ-01 | `source_type=local` 创建项目 | P0 | `201`，`status=created` |
| PROJ-02 | `source_type=repository` 创建项目 | P0 | `201` |
| PROJ-03 | 仓库地址含内联凭证 | P0 | `422 SOURCE_CREDENTIAL_FORBIDDEN` |
| PROJ-04 | `source_type` 与地址格式不匹配 | P0 | `422 SOURCE_PATH_INVALID` |
| PROJ-05 | 必填字段缺失/空字符串 | P0 | `422 VALIDATION_ERROR`，含字段明细 |
| PROJ-06 | 环境类型未启用 | P0 | `409 ENVIRONMENT_TYPE_DISABLED` |
| PROJ-07 | 查询项目列表 | P0 | `200`，普通用户仅返回本人项目 |
| PROJ-08 | 按状态/关键词筛选 | P1 | 返回匹配结果 |
| PROJ-09 | 查询项目详情 | P0 | 含 `task_content`、`statistics`、`runtime` |
| PROJ-10 | 查询不存在项目 | P0 | `404 PROJECT_NOT_FOUND` |
| PROJ-11 | 越权访问他人项目 | P0 | `403 PROJECT_ACCESS_DENIED` |
| PROJ-12 | `page_size` 超限 | P1 | `422` |
| PROJ-13 | 启动 `created` 项目 | P0 | `202`，创建运行实例和 5 条 `idle` 阶段 |
| PROJ-14 | 重复启动/启动非 `created` | P0 | `409 PROJECT_STATUS_CONFLICT` |
| PROJ-15 | 同幂等键重试启动 | P0 | `202`，返回首次结果，不重复创建任务 |
| PROJ-16 | 幂等键跨项目复用 | P0 | `409 IDEMPOTENCY_KEY_REUSED` |
| PROJ-17 | 达并发上限时启动 | P1 | `409 PROJECT_CAPACITY_EXCEEDED` |
| PROJ-18 | 停止 `running` 项目 | P0 | `202`，`stop_requested_at` 被设置，收敛为 `stopped`，已保存数据保留 |
| PROJ-19 | 停止非 `running` 项目 | P0 | `409 PROJECT_NOT_RUNNING` |
| PROJ-20 | 删除非运行项目（名称确认正确） | P0 | `202`，异步级联清理 |
| PROJ-21 | 删除运行中项目 | P0 | `409 PROJECT_DELETE_FORBIDDEN` |
| PROJ-22 | 删除确认名称不匹配 | P0 | `409 PROJECT_NAME_CONFIRMATION_MISMATCH` |
| PROJ-23 | 删除部分文件失败 | P1 | 不删除 `projects` 行，返回失败项，同幂等键可重试 |

## 3. 阶段与角色任务

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| STAGE-01 | 查询项目阶段 | P0 | 5 个阶段，按 `stage_order` 升序；未启动返回 `404` |
| STAGE-02 | 阶段严格串行推进 | P0 | 前阶段 `success` 后才进入下一阶段 |
| STAGE-03 | 阶段失败后不创建后续任务 | P0 | 项目收敛为 `failed` |
| STAGE-04 | 查询/筛选角色任务 | P0 | 分页返回，支持按阶段/角色/状态筛选 |
| STAGE-05 | 任务失败记录脱敏错误 | P0 | `error_message` 非空，不含堆栈 |
| STAGE-06 | 停止后不新增角色任务 | P0 | 已执行任务记录取消原因 |
| STAGE-07 | 角色任务幂等 | P0 | 同键重复投递不重复执行副作用 |

## 4. 实时监控

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| MON-01 | WebSocket 建连成功 | P0 | `101`，接收事件 |
| MON-02 | 未登录/无权限建连 | P0 | `401`/`403` |
| MON-03 | 接收 8 类事件 | P0 | `project_status`、`stage_status`、`worker_status`、`chat_message`、`runtime_log`、`resource_usage`、`vulnerability_found`、`report_ready` |
| MON-04 | 心跳 ping/pong | P1 | 服务端返回当前时间 |
| MON-05 | `event_id` 去重 | P0 | 重复事件仅处理一次 |
| MON-06 | 断线重连续传（`after_sequence`） | P0 | 从断点恢复，不丢不重 |
| MON-07 | 超出保留窗口（4009） | P0 | 关闭后 REST 全量补偿 |
| MON-08 | 关闭码处理 | P0 | 4001→登录页；4003→403页；4008→等待重试；4010→降频后重连 |
| MON-09 | 查询消息/日志/资源 | P0 | 游标分页，日志支持级别筛选，资源含单位说明 |
| MON-10 | 日志/消息/事件不含敏感信息 | P0 | 无密码、Cookie、Token、证书、绝对路径 |

## 5. 漏洞管理

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| VULN-01 | 查询漏洞列表/详情 | P0 | 分页列表，详情含位置、证据、验证状态 |
| VULN-02 | 按风险等级/验证状态筛选 | P1 | 返回匹配结果 |
| VULN-03 | 漏洞不存在或跨项目访问 | P0 | `404 VULNERABILITY_NOT_FOUND`（不可区分无权限） |
| VULN-04 | 同源漏洞去重 | P0 | `evidence_fingerprint` 唯一约束阻止重复 |
| VULN-05 | 指纹计算稳定 | P0 | 相同输入始终产生相同 SHA-256 |
| VULN-06 | 验证状态流转 (`pending→verified/rejected`) | P0 | 状态正确；终态保留记录 |
| VULN-07 | 风险排序：`critical > high > medium > low > info` | P1 | 同级按时间稳定排序 |

## 6. 攻击路径

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| PATH-01 | 查询路径列表/详情 | P0 | 步骤按 `step_order` 升序，含关联漏洞摘要 |
| PATH-02 | 路径不存在/跨项目 | P0 | `404 ATTACK_PATH_NOT_FOUND` |
| PATH-03 | 跨项目关联漏洞 | P0 | 复合外键约束拒绝 |
| PATH-04 | 步骤必须从 1 连续 | P0 | 跳跃或缺失被延迟约束拒绝 |
| PATH-05 | 至少一个步骤 | P0 | 空路径提交被延迟约束拒绝 |
| PATH-06 | 同路径不重复关联同漏洞 | P1 | `(path_id, vuln_id)` 唯一约束拒绝 |

## 7. 报告

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| REP-01 | 查询报告（生成中/已就绪/失败/不存在） | P0 | 正确返回各状态对应字段 |
| REP-02 | 下载 Markdown/HTML | P0 | 正确 `Content-Type`、`Content-Disposition`、`Digest` |
| REP-03 | 报告未就绪时下载 | P0 | `409 REPORT_NOT_READY` |
| REP-04 | HTML 不含脚本/事件属性 | P0 | 经允许列表清理 |
| REP-05 | 文件完整性校验 | P0 | `Digest` 与 `content_sha256` 一致，不一致返回 `500` |
| REP-06 | 报告生成失败 | P0 | `status=failed`，漏洞/路径数据仍可查询 |
| REP-07 | 下载需校验项目权限 | P0 | 越权返回 `403` |

## 8. 系统配置

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| CONF-01 | 管理员查询/更新配置 | P0 | `200`，不含密钥；版本递增，旧版 `is_active=false` |
| CONF-02 | 普通用户访问 | P0 | `403 ADMIN_REQUIRED` |
| CONF-03 | 乐观锁版本冲突 | P0 | `409 CONFIG_VERSION_CONFLICT`，返回当前版本 |
| CONF-04 | 配置值不合法 | P1 | `422` |
| CONF-05 | 同一时刻仅一个生效版本 | P0 | 部分唯一索引拒绝 |
| CONF-06 | 新配置不影响运行中项目 | P0 | 运行中项目使用启动时快照 |

## 9. 安全

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| SEC-01 | 密码/哈希不入日志、不入响应 | P0 | 审计和应用日志无明文/哈希 |
| SEC-02 | 500 错误不含堆栈/SQL/路径/密钥 | P0 | 仅 `INTERNAL_ERROR` + `request_id` |
| SEC-03 | 路径穿越 (`../../etc/passwd`) | P0 | 校验拒绝 |
| SEC-04 | 命令注入 (`; rm -rf /`) | P0 | 参数数组模式，策略拒绝 |
| SEC-05 | 日志/证据/报告 XSS 防护 | P0 | 文本转义，不执行脚本 |
| SEC-06 | 源码只读挂载 | P0 | 容器内无法修改 |
| SEC-07 | 项目间容器隔离 | P0 | 无法访问其他项目文件 |
| SEC-08 | CSRF Token 不进入 URL/日志 | P0 | 仅 Cookie + Header 传递 |
| SEC-09 | `asa_session` HttpOnly + Secure | P0 | 登录响应 Set-Cookie 检查 |
| SEC-10 | 仓库地址脱敏 | P0 | API 和数据库不含内联凭证 |

## 10. 数据一致性

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| DATA-01 | 状态变更与 Outbox 事件同事务 | P0 | 无"状态已变但事件丢失" |
| DATA-02 | `sequence` 项目内严格递增 | P0 | 无重复无跳跃 |
| DATA-03 | 删除项目级联清理 | P0 | 阶段/任务/漏洞/路径/日志/事件级联删除；审计 `SET NULL` |
| DATA-04 | `statistics` 各计数与实际记录一致 | P0 | 无偏差 |
| DATA-05 | 攻击路径复合外键阻止跨项目 | P0 | SQL 直接插入被拒绝 |
| DATA-06 | 消息/日志外键完整性 | P1 | `worker_task_id` 非空时 `stage_id` 必非空 |

## 11. 异常与边界

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| ERR-01 | 空项目列表 | P1 | `200`，`items=[]`，`total=0` |
| ERR-02 | JSON 格式错误/未知字段 | P1 | `400`/`422` |
| ERR-03 | UUID 格式无效 | P0 | `422` |
| ERR-04 | `page=0`/游标无效/时间范围倒置 | P1 | `422` |
| ERR-05 | 依赖不可用（PG/Redis） | P1 | `503 DEPENDENCY_UNAVAILABLE` |
| ERR-06 | 服务重启后状态恢复 | P0 | 数据完整可查，后台任务不受影响 |

## 12. 前端

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| FE-01 | 路由守卫：未初始化→`/system/init`，未登录→`/login` | P0 | 正确重定向 |
| FE-02 | 普通用户访问 `/system/config` | P0 | 进入 403 页 |
| FE-03 | 列表/详情各状态展示（加载/空态/错误/数据） | P0 | 骨架屏/空态提示/可重试错误 |
| FE-04 | 按钮按项目状态禁用/启用 | P0 | `created`可启动删除；终态无"重跑"按钮 |
| FE-05 | 停止/删除二次确认 | P0 | 删除须输入名称确认 |
| FE-06 | 写操作防重复提交 | P0 | 处理中按钮禁用，仅发一次请求 |
| FE-07 | 日志自动滚动/暂停/级别筛选 | P1 | 上滚暂停，出现"回到底部" |
| FE-08 | WebSocket 连接状态指示（连接中/实时/重连中/断开） | P0 | 5 种状态正确区分 |
| FE-09 | 项目切换清理旧 WebSocket 和请求 | P1 | 无残留数据 |
| FE-10 | 1440px 布局完整，1024px 可用 | P0/P1 | 无溢出，主操作可见 |
| FE-11 | 键盘操作核心流程（Tab/Enter/Escape） | P1 | 登录/创建/停止/删除可完成 |

## 13. 后端

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| BE-01 | 项目状态机：合法迁移通过，非法迁移拒绝 | P0 | 条件更新行数=0 返回 409 |
| BE-02 | 阶段状态机：`idle→running→success/failed` | P0 | 终态不可再变 |
| BE-03 | 普通用户查询自动附加 `created_by` 过滤 | P0 | 无法越权 |
| BE-04 | Domain 层无 FastAPI/SQLAlchemy/Celery 导入 | P0 | 代码审查通过 |
| BE-05 | Alembic `upgrade head` 空库执行通过 | P0 | 与 `初始化sql.md` 一致 |
| BE-06 | Celery 幂等（同键不重复执行） | P0 | 已成功直接返回缓存结果 |
| BE-07 | Celery 重试（死锁/网络瞬断）与不重试（参数/权限错误） | P0 | 有限指数退避 vs 直接失败 |
| BE-08 | Outbox 发布成功/失败重试 | P0 | `published` 标记或 `retry_count`+`next_retry_at` |
| BE-09 | 启动接口 3 秒内返回 `202` | P0 | 不等待源码拉取和容器创建 |
| BE-10 | 健康检查不含连接串/密钥/Docker 状态 | P1 | 存活仅判断进程；就绪含 PG/Redis |

## 14. 端到端

| 编号 | 用例 | 级别 | 预期结果 |
| --- | --- | :---: | --- |
| E2E-01 | 初始化→登录→项目列表 | P0 | 全流程无报错 |
| E2E-02 | 创建项目→启动→监控→查看漏洞/路径→预览报告→下载 | P0 | 各阶段成功，结果完整 |
| E2E-03 | 项目运行中停止→收敛 `stopped`→数据保留 | P0 | 阶段不推进，已保存可查 |
| E2E-04 | 停止后删除→名称确认→级联清理 | P0 | 项目从列表消失 |
| E2E-05 | WebSocket 断线自动重连 | P0 | 不丢数据；超窗口则 REST 补偿 |

---

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| V1.1 | 2026-07-24 | 精简版，共 100 条核心用例 |
