本文档约 3000 字，阅读本文档约 6 分钟。

# 前端单元测试 — 测试报告

> 测试框架：Vitest + @vue/test-utils  
> 测试环境：happy-dom  
> 运行命令：`pnpm run --filter @asa/ui test:unit`  
> 更新日期：2026-07-24

---

## 1. 测试总览

| 类别 | 文件 | 用例数 | 状态 |
| --- | --- | ---: | :---: |
| **shared/utils** | `format.spec.ts` | 27 | ✅ |
| **shared/utils** | `status-maps.spec.ts` | 13 | ✅ |
| **shared/composables** | `useAsyncState.spec.ts` | 11 | ✅ |
| **shared/composables** | `useIdempotencyKey.spec.ts` | 6 | ✅ |
| **shared/components** | `AppStatusTag.spec.ts` | 10 | ✅ |
| **shared/components** | `AppEmptyState.spec.ts` | 9 | ✅ |
| **shared/components** | `AppConfirmDialog.spec.ts` | 13 | ✅ |
| **stores** | `auth.store.spec.ts` | 25 | ✅ |
| **services/http** | `errors.spec.ts` | 10 | ✅ |
| **services/http** | `csrf.spec.ts` | 5 | ✅ |
| **services/http** | `client.spec.ts` | 8 | ✅ |
| **合计** | **11 文件** | **137** | |

---

## 2. shared/utils/format.spec.ts — 格式化工具

**覆盖函数**: `formatDateTime`, `formatDate`, `formatTime`, `formatBytes`, `formatCpuPercent`, `formatTokenCount`, `formatDuration`

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| FMT-01 | `formatDateTime(null/undefined/'' )` | 返回 `-` |
| FMT-02 | `formatDateTime('2026-07-24T08:30:00Z')` | 包含日期和时间 |
| FMT-03 | `formatDate(null)` | 返回 `-` |
| FMT-04 | `formatDate('2026-07-24T08:30:00Z')` | 返回 `2026-07-24` |
| FMT-05 | `formatTime(null)` | 返回 `-` |
| FMT-06 | `formatTime('2026-07-24T08:30:00Z')` | 包含时间 |
| FMT-07 | `formatBytes(null/undefined)` | 返回 `-` |
| FMT-08 | `formatBytes(-1/-1024)` | 返回 `-`（负值） |
| FMT-09 | `formatBytes(0)` | `0 B` |
| FMT-10 | `formatBytes(500)` | `500 B` |
| FMT-11 | `formatBytes(1024)` | `1.0 KB` |
| FMT-12 | `formatBytes(1536)` | `1.5 KB`（小数 KB） |
| FMT-13 | `formatBytes(1048576)` | `1.0 MB` |
| FMT-14 | `formatBytes(1572864)` | `1.5 MB`（小数 MB） |
| FMT-15 | `formatBytes(1073741824)` | `1.0 GB` |
| FMT-16 | `formatBytes(1099511627776)` | `1.0 TB` |
| FMT-17 | `formatCpuPercent(null/undefined)` | 返回 `-` |
| FMT-18 | `formatCpuPercent(132.456)` | `132.5%` |
| FMT-19 | `formatTokenCount(null)` | 返回 `-` |
| FMT-20 | `formatTokenCount(500)` | `500` |
| FMT-21 | `formatTokenCount(28450)` | `28.4K` |
| FMT-22 | `formatTokenCount(2500000)` | `2.5M` |
| FMT-23 | `formatDuration(null)` | 返回 `-` |
| FMT-24 | 0~60 秒前 | 包含 "秒" |
| FMT-25 | 30 分钟前 | 包含 "分钟"，不含 "小时" |
| FMT-26 | 3 小时前 | 包含 "小时" 和 "分钟" |
| FMT-27 | 2 天前 | 包含 "天" 和 "小时" |

---

## 3. shared/utils/status-maps.spec.ts — 状态映射

**覆盖导出**: `PROJECT_STATUS_MAP`, `STAGE_NAME_MAP`, `STAGE_STATUS_MAP`, `TASK_STATUS_MAP`, `WORKER_ROLE_MAP`, `RISK_LEVEL_MAP`, `VERIFY_STATUS_MAP`, `LOG_LEVEL_MAP`, `REPORT_STATUS_MAP`, `CONTAINER_STATUS_MAP`, `SOURCE_TYPE_MAP`, `RISK_LEVEL_ORDER`, `PROJECT_ALLOWED_ACTIONS`

| 编号 | 用例 | 验证点 |
| --- | --- | --- |
| SM-01 | PROJECT_STATUS_MAP | 5 个状态均有 `text` 和 `color` |
| SM-02 | STAGE_NAME_MAP | 5 个阶段按序存在 |
| SM-03 | RISK_LEVEL_MAP | 5 个级别均有 `ariaLabel` |
| SM-04 | RISK_LEVEL_ORDER | 排序权重：critical < high < medium < low < info |
| SM-05 | VERIFY_STATUS_MAP | 3 个状态：pending/verified/rejected |
| SM-06 | LOG_LEVEL_MAP | 4 个级别：debug/info/warning/error |
| SM-07 | REPORT_STATUS_MAP | 4 个状态：pending/generating/ready/failed |
| SM-08 | CONTAINER_STATUS_MAP | 7 个状态 |
| SM-09 | WORKER_ROLE_MAP | 6 个角色 |
| SM-10 | SOURCE_TYPE_MAP | 2 个类型：local/repository |
| SM-11 | PROJECT_ALLOWED_ACTIONS | `created` 含 start/delete；`running` 含 stop/monitor；`completed` 含 report |
| SM-12 | STAGE_STATUS_MAP | 4 个状态 |
| SM-13 | TASK_STATUS_MAP | 4 个状态 |

---

## 4. shared/composables/useAsyncState.spec.ts — 异步状态管理

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| AS-01 | 初始化状态 | `data=null`, `loading=false`, `error=null` |
| AS-02 | 执行中 loading 为 true | 调用后立即 `loading=true` |
| AS-03 | 成功后设置 data | `result === data.value` |
| AS-04 | 参数透传 | `fn` 收到 `execute` 的原始参数 |
| AS-05 | ApiError 直接赋值 | `error.value` 即为抛出的 ApiError |
| AS-06 | 非 ApiError 包装为 ApiError | `code='UNKNOWN'`, `message` 来自 Error |
| AS-07 | 字符串 reject 处理 | `code='UNKNOWN'`, `message='未知错误'` |
| AS-08 | 过期响应丢弃（竞态条件） | 后续调用先返回 → 前调用结果不覆盖 data |
| AS-09 | 过期错误丢弃 | 旧请求的错误不覆盖当前无错状态 |
| AS-10 | reset 清空全部状态 | 三字段均置 null/false |
| AS-11 | reset 后 stale 不更新 loading | 请求在 reset 后完成，loading 保持 false |

---

## 5. shared/composables/useIdempotencyKey.spec.ts — 幂等键管理

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| IK-01 | 初始化 key 为 null | `key.value === null` |
| IK-02 | generate 创建新 key | UUID 格式，更新 `key.value` |
| IK-03 | getOrCreate 复用 | 多次调用返回同一个 key |
| IK-04 | reset 后 getOrCreate 生成新 key | 新 key ≠ 旧 key |
| IK-05 | reset 清空 key | `key.value === null` |
| IK-06 | 连续 generate 产生唯一 UUID | 3 次调用的 key 互不相同 |

---

## 6. shared/components/AppStatusTag.spec.ts — 状态标签组件

**Props**: `value`, `map`, `size`

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| ST-01 | 已知值渲染文本 | 显示 map 中对应 text |
| ST-02 | 未知值 fallback | 显示 "未知状态" |
| ST-03 | aria-label 来自 map | 使用 map 中定义的值 |
| ST-04 | aria-label fallback 为 text | 无 ariaLabel 时使用 text |
| ST-05 | size 类名 | `size='small'` → `status-tag--small` |
| ST-06 | 默认 size | `status-tag--default` |
| ST-07 | dot + text 子元素 | 两个子元素均存在 |
| ST-08 | CSS 变量设置 | style 含 `--tag-color` |
| ST-09 | 全部项目状态 | 5 个状态均正确渲染文本 |
| ST-10 | 全部风险等级 | 5 个级别均正确渲染文本 |

---

## 7. shared/components/AppEmptyState.spec.ts — 空状态组件

**Props**: `title`, `description`, `actionText` | **Emits**: `action`

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| ES-01 | 默认标题 | 显示 "暂无数据" |
| ES-02 | 自定义标题 | 显示传入的 title |
| ES-03 | 无 description 时不渲染 | `.empty-state__description` 不存在 |
| ES-04 | 有 description 时渲染 | 元素存在且文本匹配 |
| ES-05 | actionText 为空无按钮 | `.empty-state__action` 不存在 |
| ES-06 | actionText 非空渲染按钮 | 按钮存在且文本匹配 |
| ES-07 | 点击按钮 emit action | `emitted('action')` 触发 |
| ES-08 | 图标元素存在 | el-icon stub 渲染 |
| ES-09 | 完整 props 渲染三部分 | title + description + actionText 均显示 |

---

## 8. shared/components/AppConfirmDialog.spec.ts — 确认对话框组件

**Props**: `visible`, `title`, `message`, `confirmText`, `confirmType`, `requireNameInput`, `expectedName`, `loading`  
**Emits**: `confirm`, `cancel`, `update:visible`

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| CD-01 | 渲染 title 和 message | 文本匹配 |
| CD-02 | 默认确认按钮文本 | "确定" |
| CD-03 | 自定义确认文本 | 显示传入的 confirmText |
| CD-04 | 取消按钮存在 | 显示 "取消" |
| CD-05 | 默认无名称输入框 | `.confirm-dialog__input` 不存在 |
| CD-06 | requireNameInput 时显示输入框 | 元素存在且含 expectedName |
| CD-07 | 名称不匹配时确认按钮禁用 | disabled 属性存在 |
| CD-08 | 名称不匹配显示错误 | "项目名称不匹配" |
| CD-09 | 名称匹配时确认按钮可用 | 无 disabled 属性 |
| CD-10 | loading 时确认按钮禁用 | disabled 属性存在 |
| CD-11 | 点击确认 emit confirm | `emitted('confirm')` 触发 |
| CD-12 | 点击取消 emit cancel | `emitted('cancel')` 触发 |
| CD-13 | visible=false 不渲染对话框 | 无可渲染内容 |

---

## 9. stores/auth.store.spec.ts — 认证 Store (Pinia)

**覆盖**: state, getters, actions

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| AUTH-01 | 初始 user=null | 未认证 |
| AUTH-02 | 初始 isSystemInitialized=null | 状态未知 |
| AUTH-03 | 初始 isAuthenticated=false | 未认证 |
| AUTH-04 | 初始 isAdmin=false | 非管理员 |
| AUTH-05 | 初始 loadingStatus='idle' | 空闲状态 |
| AUTH-06 | 初始 isBootstrapped=false | 未启动 |
| AUTH-07 | checkSystemStatus 成功 | initialized=true, loadingStatus='idle' |
| AUTH-08 | checkSystemStatus 未初始化 | initialized=false |
| AUTH-09 | checkSystemStatus 网络失败 | 抛出错误 + loadingStatus='error' |
| AUTH-10 | checkSystemStatus 加载态 | loadingStatus='loading' |
| AUTH-11 | initSystem 成功 | 返回 admin + initialized=true |
| AUTH-12 | login 成功 | 设置 user + isAuthenticated=true |
| AUTH-13 | login 失败 | 抛出 ApiError + user 不变 |
| AUTH-14 | logout API 失败仍清状态 | user=null |
| AUTH-15 | clearSession 清理 asa_ 前缀 sessionStorage | asa_ 键移除，其他保留 |
| AUTH-16 | clearSession 设置 user=null | |
| AUTH-17 | clearSession 仅移除 asa_ 键 | 非 asa_ 键保留 |
| AUTH-18 | isAuthenticated=true when user exists | |
| AUTH-19 | isAdmin=false for role=user | |
| AUTH-20 | isAdmin=true for role=admin | |
| AUTH-21 | username='' when no user | |
| AUTH-22 | userId='' when no user | |
| AUTH-23 | enableDevMode | dev_admin + isAdmin=true + initialized=true |
| AUTH-24 | markBootstrapped | isBootstrapped=true |
| AUTH-25 | setSystemStatusUnknown | initialized=null + error 状态 |

---

## 10. services/http/errors.spec.ts — API 错误类

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| ERR-01 | 构造全字段 ApiError | 所有字段正确赋值 |
| ERR-02 | isAuthError=401 | true |
| ERR-03 | isForbidden=403 | true |
| ERR-04 | isNotFound=404 | true |
| ERR-05 | isConflict=409 | true |
| ERR-06 | isValidationError=422 | true |
| ERR-07 | isRateLimited=429 | true + retryAfter |
| ERR-08 | isServerError=500 | true |
| ERR-09 | getFieldError 匹配/不匹配/未定义 | 正确返回/undefined |
| ERR-10 | 默认值（fieldErrors=[], retryAfter=null, data=null） | |

---

## 11. services/http/csrf.spec.ts — CSRF Token 读取

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| CSRF-01 | 空 Cookie | null |
| CSRF-02 | 仅 asa_csrf | 返回 token |
| CSRF-03 | 多个 Cookie 含 asa_csrf | 正确提取 |
| CSRF-04 | Cookie 不含 asa_csrf | null |
| CSRF-05 | asa_csrf=空值 | 返回 `''` |

---

## 12. services/http/client.spec.ts — HTTP 客户端

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| CL-01 | unwrap 提取 data | 从 ApiResponse 中解包 |
| CL-02 | unwrap null data | 返回 null |
| CL-03 | apiGet 带参数 | 调用 axios.get + 解包 |
| CL-04 | apiGet 无参数 | 调用 axios.get 不带 params |
| CL-05 | apiPost 带 body | 调用 axios.post + 解包 |
| CL-06 | apiPost 无 body | 调用 axios.post(undefined) |
| CL-07 | apiPut | 调用 axios.put + 解包 |
| CL-08 | apiDelete | 调用 axios.delete(data) + 解包 |

---

## 13. 测试覆盖率报告

### 运行覆盖率命令

```bash
pnpm run --filter @asa-ui vitest run --coverage
```

### 覆盖文件统计

| 类别 | 源文件 | 已覆盖 | 覆盖率 |
| --- | --- | :---: | :---: |
| shared/utils | 2 | 2 | 100% |
| shared/composables | 2 | 2 | 100% |
| shared/components | 6 | 3 | 50% |
| services/http | 3 | 3 | 100% |
| stores | 1 | 1 | 100% |
| modules/**/api | 7 | 0 | 0% |
| modules/**/pages | 13 | 0 | 0% |
| services/realtime | 1 | 0 | 0% |
| router | 1 | 0 | 0% |

> **说明**: API 层（modules/**/api）均为对 `apiGet/apiPost/apiPut/apiDelete` 的薄包装，其逻辑已在 `client.spec.ts` 中覆盖。页面组件（pages）依赖真实后端数据和路由上下文，需 E2E 测试覆盖。

---

## 14. 未来测试计划

| 优先级 | 范围 | 说明 |
| :---: | --- | --- |
| P1 | `services/realtime/connection.ts` | WebSocket 连接、重连、心跳管理 |
| P1 | `router/index.ts` | 路由守卫、UUID 校验、重定向 |
| P2 | `AppLoadingSkeleton.vue` | 骨架屏渲染 |
| P2 | `AppErrorBlock.vue` | 错误展示与重试 |
| P2 | `AppPageHeader.vue` | 页面标题栏 |
| P3 | E2E (Playwright/Cypress) | 关键用户流程、WebSocket 实时更新 |

---

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| V1.0 | 2026-07-24 | 初版，11 个测试文件，137 个用例 |
